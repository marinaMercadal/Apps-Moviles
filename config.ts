// Cambiar esta IP si el backend corre en otra compu o red
// Tiene que ser la IP local del backend (mismo wifi)

export const API_ORIGIN = "http://172.29.135.101:3000";
export const API_URL = `${API_ORIGIN}/api`;
